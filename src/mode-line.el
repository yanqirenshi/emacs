;;;;;
;;;;; モードライン
;;;;;
;; カーソルがある行の行数を表示
(line-number-mode t)
;; カーソルがある位置の文字数を表示
(column-number-mode t)
;; 時刻を表示する。
(setq display-time-format "%Y-%m-%d (%a) %H:%M")
(display-time-mode)
;; バッテリー状態
(display-battery-mode)
;; ファイルのサイズ
(size-indication-mode)
